# The mobile app reads the API base from src/api/client.ts (API_BASE).
# For a physical device on your LAN, set it to the backend host's IP, e.g.:
#   http://192.168.5.57:4000
# Android emulator -> http://10.0.2.2:4000
# iOS simulator    -> http://:4000
