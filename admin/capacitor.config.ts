import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vmukti.fieldtracking',
  appName: 'field-tracking-platform',
  webDir: 'dist'
};

export default config;
